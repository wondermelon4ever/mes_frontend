import React from 'react';
import ReactDOM from 'react-dom';

import PutTogether from "./PutTogether";

const rootElement = document.getElementById('root');

ReactDOM.render(<PutTogether />, rootElement);